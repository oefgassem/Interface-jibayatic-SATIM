services:
  reverse-proxy:
    image: nginx:alpine
    container_name: qas-reverse-proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
